{
  "name": "proof-of-existence",
  "version": "1.0.0",
  "description": "Blockchain-backed Proof of Existence system with Web2 UX",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "deploy:contract": "cd contracts && npx hardhat run scripts/deploy.js --network sepolia",
    "verify:contract": "cd contracts && npx hardhat verify --network sepolia"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}